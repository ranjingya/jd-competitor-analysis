[project]
name = "jd-competitor-analysis-runtime"
version = "0.1.0"
description = "京东自营竞品分析 Skill 的 Python 运行环境"
requires-python = ">=3.10"
dependencies = [
    "openpyxl>=3.1.5,<4",
]

[tool.uv]
package = false
